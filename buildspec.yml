version: 0.2
 
phases:
  install:
    runtime-versions:
      nodejs: 22
    commands:
      - npm install -g artillery@latest
  build:
    runtime-versions:
      nodejs: 22
    commands:
      - artillery run index.ts